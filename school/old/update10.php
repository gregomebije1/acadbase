CREATE TABLE directory (
  id int(11) NOT NULL auto_increment,
  name varchar(300),
  address text,
  phone varchar(100),
  email varchar(100),
  web varchar(100),
  logo varchar(100), 
  other_information text
);
